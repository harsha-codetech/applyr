{
  "name": "applyr",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension that fills job applications from a local profile. No build step, no dependencies.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "zip": "node scripts/package.js",
    "serve": "node scripts/serve.js"
  },
  "engines": {
    "node": ">=20"
  }
}
